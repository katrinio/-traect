name: 🐳 Build and Push Docker Image

on:
  push:
    branches:
      - main
      - public-flow
    paths:
      - 'pyproject.toml'
      - 'poetry.lock'
      - 'Dockerfile'
      - 'src/**'
      - 'migrations/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract repo name
        id: repo
        run: |
          repo_name=$(echo ${{ github.repository }} | cut -d'/' -f2)
          # Remove leading dash if present
          repo_name=${repo_name#-}
          echo "name=$repo_name" >> $GITHUB_OUTPUT

      - name: Build and push production image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          target: production
          tags: |
            ghcr.io/${{ github.repository_owner }}/${{ steps.repo.outputs.name }}:latest
            ghcr.io/${{ github.repository_owner }}/${{ steps.repo.outputs.name }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push test image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          target: test
          tags: |
            ghcr.io/${{ github.repository_owner }}/${{ steps.repo.outputs.name }}:test-latest
            ghcr.io/${{ github.repository_owner }}/${{ steps.repo.outputs.name }}:test-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
